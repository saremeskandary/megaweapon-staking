import React from "react";
import Layout from "../components/Layout";

type Props = {};

export default function stake({}: Props) {
  return (
    <Layout>
      <div>stake</div>
    </Layout>
  );
}
